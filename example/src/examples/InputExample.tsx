import React from 'react';
// eslint-disable-next-line import/no-unresolved
import { Input } from 'supergroww';
import { SafeAreaView } from 'react-native-safe-area-context';

const InputExample = () => {
  return (
    <SafeAreaView>
      <Input />
    </SafeAreaView>
  );
};

export default InputExample;
